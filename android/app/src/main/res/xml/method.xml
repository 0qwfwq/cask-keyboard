<?xml version="1.0" encoding="utf-8"?>
<!--
  Describes the input method to the system. Referenced from the <service> meta-data
  (android.view.im) in AndroidManifest.xml. Each <subtype> is a layout/language the
  keyboard offers; here we ship a single English (US) keyboard subtype.
-->
<input-method xmlns:android="http://schemas.android.com/apk/res/android"
    android:supportsSwitchingToNextInputMethod="true">
    <subtype
        android:label="@string/subtype_label"
        android:imeSubtypeLocale="en_US"
        android:imeSubtypeMode="keyboard" />
</input-method>

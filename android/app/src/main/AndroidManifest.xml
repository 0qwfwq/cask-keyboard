<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Key-press haptics (configurable in the setup app). Normal permission, no prompt. -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- GIF search (GIPHY) + downloading the chosen GIF so it can be inserted. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Voice-to-type (hold the spacebar): records audio while dictating, on-device where available. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- Adaptive theming reads the foreground app's icon/theme colour. Package-visibility
         filtering (Android 11+) otherwise hides other apps from us. This is flagged by Google
         Play if you publish; it is fine for a personal / sideloaded keyboard. -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <application
        android:label="cask"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <!-- Invisible helper the keyboard launches to request the mic permission (an IME can't
             prompt for runtime permissions itself). Translucent + no history so it never appears. -->
        <activity
            android:name=".voice.VoicePermissionActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" />

        <!-- The keyboard itself. The system binds to this service; the BIND_INPUT_METHOD
             permission ensures only the OS can do so. The intent-filter + meta-data register
             it as an available input method. -->
        <service
            android:name=".CaskKeyboardService"
            android:label="@string/keyboard_label"
            android:permission="android.permission.BIND_INPUT_METHOD"
            android:exported="true">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method" />
        </service>

        <!-- Serves downloaded GIFs to the target app via a content:// URI for rich-content insert. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.gifs"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
